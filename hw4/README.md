# `myserver`: A Simple HTTP Server

# Build
Use `make` to build all necessary stuff.

# Usage
- `./myserver <port> <path/to/docroot>`
- ex: `./myserver 30080 .`

# Feature
We currently support all basic feature.

You can close the http server by Ctrl+C
